function __(el){return document.querySelectorAll(el); }
function overIcon(u,e,c){
  e.setAttribute('src', u + c + '.svg');
}
function slider(c){
  var s = __('.slide');
  var len = s.length;
  switch (c) {
    case 'prev':
      
    break;
    case 'next':
      
    break;
  }
}
